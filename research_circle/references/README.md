# references/

This directory stores external reference repositories cloned via SSH only.

Use:

```bash
./bin/clone_references.sh
```

Do not edit upstream source here. Use this folder for reading and extracting design patterns only.
